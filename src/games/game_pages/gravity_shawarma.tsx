import React from "react";
import FlashTemplate from "../game_template";

// eslint-disable-next-line @typescript-eslint/naming-convention
const GravityShawarma: React.FC = () => {
  return (
    <FlashTemplate src={"flash/gravity_shawarma.swf"} title={"Всемирное тяготение и шаурма"}/>);
}

export default GravityShawarma;
